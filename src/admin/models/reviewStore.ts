export interface ReviewStore {
  store_name: number;
  commenter: string;
  comment: string;
  email: string;
  contributor: number;
  created_at: string;
  customer: object;
  store: object;
  title: string;
  content: string;
  star: number;
  reply: object;
}
